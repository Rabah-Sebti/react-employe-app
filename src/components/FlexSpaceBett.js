import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexSpaceBett = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});
export default FlexSpaceBett;
